{{# each products}}
<div class="row">
  {{# each this}}
    <div class="col-sm-6 col-md-4">
      <div class="thumbnail">
        <img src="{{this.imagePath}}" alt="Flail">
        <div class="caption">
          <h3>{{this.title}}</h3>
          <div class='clearfix'>
            <p class="price pull-left">${{this.price}}</p>
            <p><a href="#" class="btn btn-primary pull-right" role="button">Button</a></p>
          </div>
        </div>
      </div>
    </div>
  {{/each}}
</div>
{{/each}}
